// import { useNavigate } from "react-router-dom";
// import { PROTECTED_ROUTES, RouteName } from "./router";

export const isProtectedRoute = (routeName: unknown) => {
  //   const navigate = useNavigate();
  //   PROTECTED_ROUTES.includes(routeName) ? navigate(RouteName.ERROR_403) : "";
};
